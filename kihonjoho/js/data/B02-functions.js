/* =============================================================
   コレダケ学習 基本情報 科目B — 02 手続・関数と再帰
   ============================================================= */
window.CURRICULUM = window.CURRICULUM || [];
window.CURRICULUM.push(
  {
    id: "feb-functions", domain: "【科目B】第1章 擬似言語の記述", icon: "🧩", title: "手続・関数と再帰",
    intro: "処理をまとめる手続・関数、値の受け渡し（引数・戻り値）、そして自分を呼ぶ再帰。トレースで確実に。",
    understand: [
      {
        h: "手続と関数——処理をまとめて名前で呼ぶ",
        body:
          "<p>よく使う処理を1つにまとめ、名前を付けて呼び出せるようにしたものが<strong>手続</strong>と<strong>関数</strong>です。違いは<strong>戻り値（呼び出し元へ返す値）があるか</strong>——関数はあり、手続はなし、と考えます。</p>" +
          "<p>呼び出すときに渡す値が<strong>引数（ひきすう）</strong>。関数の中で計算した結果を <code>return</code> で返します。下の関数 <code>足す</code> は、2つの引数の和を返します。</p>" +
          PCODE(
            "整数型: 足す(整数型: a, 整数型: b)\n" +
            "  return a + b\n" +
            "\n" +
            "整数型: 答え\n" +
            "答え ← 足す(3, 5)   // a=3, b=5 で呼び出し、8 が返る\n" +
            "// 答え は 8",
            "呼び出し時に 3 と 5 が a, b に渡され、a+b=8 が返って 答え に入る。"
          ),
      },
      {
        h: "引数の渡し方——値渡しに注意",
        body:
          "<p>科目Bの擬似言語では、基本的に<strong>引数は『値のコピー』が渡される（値渡し）</strong>と考えます。つまり<strong>関数の中で引数を書き換えても、呼び出し元の変数は変わりません</strong>。ここは引っかけとして頻出です。</p>" +
          "<p>ただし<strong>配列やオブジェクトを渡した場合は、中身（要素）を書き換えると呼び出し元にも反映される</strong>ことが多い点に注意します（同じ実体を参照するため）。問題文の説明に従って判断します。</p>",
      },
      {
        h: "再帰——自分自身を呼び出す関数",
        body:
          "<p><strong>再帰（さいき）</strong>とは、<strong>関数が自分自身を呼び出す</strong>ことです。難しく感じますが、コツは<strong>「終わりの条件（これ以上呼ばない基底条件）」と「自分を呼ぶ部分」の2つを見つける</strong>ことです。</p>" +
          "<p>下は階乗（n! ＝ n×(n−1)×…×1）を求める関数です。<code>階乗(3)</code> は <code>3 × 階乗(2)</code>、<code>階乗(2)</code>は<code>2 × 階乗(1)</code>…と、答えが返るまで呼び出しが積み重なり、<strong>基底条件（n=0で1を返す）に届いてから、逆順に掛け算が確定</strong>していきます。</p>" +
          PCODE(
            "整数型: 階乗(整数型: n)\n" +
            "  if (n = 0)          // 終わりの条件（基底条件）\n" +
            "    return 1\n" +
            "  endif\n" +
            "  return n × 階乗(n − 1)  // 自分を呼ぶ\n" +
            "\n" +
            "// 階乗(3) = 3×階乗(2) = 3×2×階乗(1) = 3×2×1×階乗(0)\n" +
            "//        = 3×2×1×1 = 6",
            "n=0 で1を返して底を打ち、そこから 1→1→2→6 と掛け算が確定する。階乗(3)=6。"
          ),
      },
    ],
    memorize: [
      { k: "手続 / 関数", v: "処理をまとめて名前で呼ぶ。関数は戻り値あり、手続は戻り値なし。" },
      { k: "引数 / 戻り値", v: "引数=呼び出し時に渡す値、戻り値=returnで返す値。" },
      { k: "値渡し", v: "引数は値のコピー。関数内で書き換えても呼び出し元の変数は変わらない。" },
      { k: "配列・オブジェクトの引数", v: "中身を書き換えると呼び出し元にも反映されることが多い（同じ実体を参照）。" },
      { k: "再帰", v: "関数が自分自身を呼ぶ。基底条件（終わりの条件）が必須。" },
      { k: "基底条件", v: "再帰を止める条件。これが無いと無限に呼び出して止まらない。" },
    ],
    flashcards: [
      { q: "手続と関数の違いは？", a: "戻り値（呼び出し元へ返す値）があるかどうか。関数はあり、手続はなし。" },
      { q: "値渡しで、関数の中で引数を書き換えると呼び出し元の変数はどうなる？", a: "変わらない（渡されるのは値のコピーだから）。" },
      { q: "再帰関数に必ず必要なものは？", a: "基底条件（これ以上自分を呼ばずに値を返して終わる条件）。無いと無限に呼び出す。" },
      { q: "階乗(4)を再帰で計算すると、どんな式に展開される？", a: "4×階乗(3)=4×3×階乗(2)=…=4×3×2×1×階乗(0)=24。" },
    ],
    quiz: [
      {
        q:
          "次の関数 f を f(4) で呼び出したとき、戻り値はどれか。" +
          PCODE(
            "整数型: f(整数型: n)\n" +
            "  if (n ≦ 1)\n" +
            "    return 1\n" +
            "  endif\n" +
            "  return n × f(n − 1)"
          ),
        choices: ["4", "10", "24", "無限に呼び出す"],
        answer: 2,
        explain: "f(4)=4×f(3)=4×3×f(2)=4×3×2×f(1)=4×3×2×1＝<strong>24</strong>（階乗）。n≦1で底を打つ。",
      },
      {
        q:
          "次のプログラムを実行したとき、最後に出力される値はどれか。手続 add は値渡しである。" +
          PCODE(
            "整数型: add(整数型: x)\n" +
            "  x ← x + 10   // 引数 x を書き換える\n" +
            "\n" +
            "整数型: a ← 5\n" +
            "add(a)\n" +
            "a を出力"
          ),
        choices: ["5", "10", "15", "エラーになる"],
        answer: 0,
        explain: "値渡しなので、手続の中で x を書き換えても呼び出し元の a は<strong>5のまま</strong>。addは戻り値も使っていない。",
      },
      {
        q:
          "次の関数 g を g(3) で呼び出したとき、戻り値はどれか。" +
          PCODE(
            "整数型: g(整数型: n)\n" +
            "  if (n = 0)\n" +
            "    return 0\n" +
            "  endif\n" +
            "  return n + g(n − 1)"
          ),
        choices: ["3", "6", "9", "0"],
        answer: 1,
        explain: "g(3)=3+g(2)=3+2+g(1)=3+2+1+g(0)=3+2+1+0＝<strong>6</strong>（1からnまでの和）。",
      },
      {
        q: "再帰関数に基底条件（終わりの条件）が書かれていない場合、実行するとどうなるか。",
        choices: [
          "自動的に1回だけ実行して終わる",
          "自分自身を延々と呼び出し続け、正常に終了しない",
          "コンパイル時に必ず値0を返す",
          "引数を無視して実行される",
        ],
        answer: 1,
        explain: "基底条件が無いと<strong>無限に自分を呼び出し続けて終わらない</strong>（スタックあふれになる）。",
      },
      {
        q:
          "関数 sum は、1 から n までの整数の和を再帰で求める。空欄 <b>a</b> に入れる正しい記述はどれか。例：sum(3)＝3+2+1＝6。" +
          PCODE(
            "整数型: sum(整数型: n)\n" +
            "  if (n = 0)\n" +
            "    return 0\n" +
            "  endif\n" +
            "  return " + BLANK("a") + "\n"
          ),
        choices: ["n + sum(n − 1)", "n × sum(n − 1)", "sum(n − 1)", "n + sum(n)"],
        answer: 0,
        explain: "『n と、n未満の和』を足すので <strong>n + sum(n − 1)</strong>。× は階乗、sum(n)のままだと基底に届かず無限再帰。",
      },
      {
        q:
          "次の再帰関数 fib（フィボナッチ数）を fib(5) で呼び出したときの戻り値はどれか。fib(1)=1, fib(2)=1, 以降は前2つの和。" +
          PCODE(
            "整数型: fib(整数型: n)\n" +
            "  if (n ≦ 2)\n" +
            "    return 1\n" +
            "  endif\n" +
            "  return fib(n − 1) + fib(n − 2)"
          ),
        choices: ["3", "5", "8", "13"],
        answer: 1,
        explain: "1,1,2,3,<strong>5</strong>,… fib(3)=2, fib(4)=3, fib(5)=fib(4)+fib(3)=3+2＝<strong>5</strong>。前2項を足していく。",
      },
    ],
  }
);
